# Spring Boot settings
spring.application.name=skillgraph
server.port=${PORT:8080}

# CognoDB settings (values should be overridden by environment variables)
cogno.db.uri=${COGNO_DB_URI:bolt://localhost:7687}
cogno.db.user=${COGNO_DB_USER:cognodb}
cogno.db.password=${COGNO_DB_PASSWORD:password}
